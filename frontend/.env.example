# Copia este archivo como .env.local si necesitas apuntar a otra URL de API.
# Por defecto (sin este archivo) el frontend usa la ruta relativa "/api",
# que funciona tanto en local (vía proxy de Vite) como en producción en cPanel.
# VITE_API_URL=https://tudominio.com/api
